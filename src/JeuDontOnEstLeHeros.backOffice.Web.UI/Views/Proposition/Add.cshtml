@model JeuDontOnEstLeHeros.Core.Data.Models.Reponse

@{
    ViewData["Title"] = "View";
    List<JeuDontOnEstLeHeros.Core.Data.Models.Question> questionsList = null;

    questionsList = this.ViewBag.QuestionList;
    var listItems = questionsList.Select(item => new SelectListItem() { Text = item.Titre, Value = item.Id.ToString() }).ToList();

}

<h1>View</h1>

<h4>Reponse</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="View">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Description" class="control-label"></label>
                <input asp-for="Description" class="form-control" />
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <!-- TODO: rammener une list :-->
                <select asp-for="QuestionId" asp-items="listItems"></select>

            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

